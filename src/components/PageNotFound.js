import React from 'react';

const PageNotFound = () => {
  return (
    <div>
      <img
        src={require('../static/NotFound.png')}
        style={{ width: '100%', height: '500px' }}
      />
    </div>
  );
};

export default PageNotFound;
